name: offline_first
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

# pubspec.yaml
dependencies:
  flutter:
    sdk: flutter
  
  # Local storage (from Part 1)
  sqflite: ^2.3.0
  path: ^1.8.3
  uuid: ^4.1.0
  intl: ^0.18.1
  json_annotation: ^4.9.0
  
  # GetX for state management and utils
  get: ^4.6.6
  
  # Connectivity monitoring
  connectivity_plus: ^6.0.5
  internet_connection_checker_plus: ^2.5.2
  
  # Shared preferences for storing sync metadata
  shared_preferences: ^2.2.2
  path_provider: ^2.1.5
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.8
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
